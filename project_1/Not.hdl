/**
    NOT

    This is a simple not gate, the single-bit output is the inverse of the single-bit input.
    1 -> 0
    0 -> 1
*/


CHIP Not {
    IN in;
    OUT out;
    
    PARTS:
        Nand(a=in, b=in, out=out);
}
